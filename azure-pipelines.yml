trigger:
 - main

pool:
  vmImage: ubuntu-latest

stages:
  - stage: build_stage
    jobs:
      - job: jobA
        pool:
          vmImage: ubuntu-latest
        steps:
          - script: echo "hello"

  - stage: test_stage
    dependsOn: build_stage
    condition: succeeded()
    jobs:
      - job: jobA
        steps:
          -  script: echo "test"
  - stage: deploy
    dependsOn: build_stage
    jobs:
      -  job: 
         steps:
           -  script: echo "deploy"
